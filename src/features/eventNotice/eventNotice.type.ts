export type eventType = {
  eventNo: number;
  bannerUrl: string;
  startDate: string;
  endDate: string;
  isActive: boolean;
};

export type noticeType = {
  noticeNo: number;
  title: string;
  noticeType: string;
  content: string;
  createdAt: string;
};
